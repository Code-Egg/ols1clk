  
name: ols1clk

on:
  push:
    branches:
      - master

  pull_request:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    container: 'ubuntu:20.04'
    steps:
      - uses: actions/checkout@v2
      - name: lsws1clk test
        run: sudo bash ols1clk.sh -W